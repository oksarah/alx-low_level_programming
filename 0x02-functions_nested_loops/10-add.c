#include "main.h"
#include <stdio.h>

/**
 * add - two intergers and returns result
 * @num1: the 1st interger
 * @num2: the 2nd interger
 * Return: 0
 */
int add(int num1, int num2)
{
return (num1 + num2);
}
